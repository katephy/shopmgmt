<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.ecommerce.dao.ShopCategoryDao">
    <select id="queryShopCategory" resultType="com.shop.ecommerce.entity.ShopCategory">
        SELECT
        shop_category_id,
        shop_category_name,
        shop_category_desc,
        shop_category_img,
        priority,
        time_created,
        time_updated,
        parent_id
        FROM
        tb_shop_category
        <where>
            <if test="shopCategoryCondition == null">
                and parent_id is null
            </if>
            <if test="shopCategoryCondition != null">
                and parent_id is not null
            </if>
            <if
                    test="shopCategoryCondition != null and shopCategoryCondition.parentId != null">
                and parent_id = #{shopCategoryCondition.parentId}
            </if>
        </where>
        ORDER BY
        priority DESC
    </select>

    <insert id="insertShopCategory" useGeneratedKeys="true"
            keyProperty="shopCategoryId" keyColumn="shop_category_id">
		INSERT INTO
		tb_shop_category(shop_category_name,shop_category_desc,shop_category_img,
		priority,time_created,time_updated,parent_id)
		VALUES
		(#{shopCategoryName},#{shopCategoryDesc},#{shopCategoryImg},
		#{priority},#{timeCreated},#{timeUpdated},#{parentId})
	</insert>

    <update id="updateShopCategory" parameterType="com.shop.ecommerce.entity.ShopCategory">
        update tb_shop_category
        <set>
            <if test="shopCategoryName != null">shop_category_name=#{shopCategoryName},</if>
            <if test="shopCategoryDesc != null">shop_category_desc=#{shopCategoryDesc},</if>
            <if test="shopCategoryImg != null">shop_category_img=#{shopCategoryImg},</if>
            <if test="priority != null">priority=#{priority},</if>
            <if test="timeUpdated != null">time_updated=#{timeUpdated},</if>
            <if test="parentId != null">parent_id=#{parentId}</if>
        </set>
        where shop_category_id=#{shopCategoryId}
    </update>

    <select id="queryShopCategoryById" resultType="com.shop.ecommerce.entity.ShopCategory">
		SELECT
		shop_category_id,
		shop_category_name,
		shop_category_desc,
		shop_category_img,
		priority,
		time_created,
		time_updated,
		parent_id
		FROM
		tb_shop_category
		WHERE
		shop_category_id=#{shopCategoryId}
	</select>
</mapper>